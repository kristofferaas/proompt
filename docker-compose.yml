version: '3'

services:
    database:
        image: postgres:16
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: password
            POSTGRES_DB: game
        ports:
            - '5432:5432'